# Copy to inventory.ini and set your host. One target host (single-node stack).
[gert]
gert-prod ansible_host=203.0.113.10 ansible_user=root

# For a non-root SSH user with sudo, instead use e.g.:
# gert-prod ansible_host=203.0.113.10 ansible_user=deploy ansible_become=true
