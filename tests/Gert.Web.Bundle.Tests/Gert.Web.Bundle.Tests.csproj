<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="$(TestSdkVersion)" />
    <PackageReference Include="xunit.v3" Version="$(XunitV3Version)" />
    <PackageReference Include="xunit.runner.visualstudio" Version="$(XunitRunnerVersion)" />
    <PackageReference Include="FluentAssertions" Version="$(FluentAssertionsVersion)" />
  </ItemGroup>

  <ItemGroup>
    <!-- The no-npm publish bundler: pinned-manifest + index.html-repoint logic under test
         (the esbuild download/extract path is covered by the publish run, not these
         hermetic unit tests). -->
    <ProjectReference Include="..\..\tools\Gert.Web.Bundle\Gert.Web.Bundle.csproj" />
  </ItemGroup>

</Project>
